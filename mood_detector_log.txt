
================================================================================
SESSION LOG — Webcam Mood Detector
Date   : April 24, 2026
File   : mood_detector.py
================================================================================

OVERVIEW
--------
Built a real-time facial emotion detection program that reads from a webcam,
classifies the visible face into one of 8 moods, and forwards mood changes as
TCP messages to a PureData [netreceive] object (or directly to rhythm_changes.py).
The program is designed to work in tandem with rhythm_changes.py, which listens
on port 3000 and uses the mood to shape its improvised melody.

Detected moods: happiness · sadness · anger · fear · surprise · disgust · contempt · neutral

Output file: mood_detector.py

--------------------------------------------------------------------------------
DESIGN DECISIONS
--------------------------------------------------------------------------------

Available vision resources on this machine:
  - llava:34b         (20 GB, Ollama — vision LLM, already pulled)
  - gemma3:12b        (8.1 GB, Ollama — vision LLM, already pulled)
  - gemma3:27b        (17 GB, Ollama — vision LLM, already pulled)
  - deepface          (not yet installed, but pip-installable)
  - opencv-python     (not yet installed, but pip-installable)

Two backends were implemented so the program works regardless of whether
deepface/TensorFlow are installed:

  Backend 1: deepface (default, recommended)
    - Uses the DeepFace library with a pre-trained FER emotion model.
    - Fast enough for near real-time use (~5–10 FPS analysis rate).
    - Returns per-emotion confidence percentages.
    - Requires:  pip install opencv-python deepface tf-keras

  Backend 2: ollama
    - Sends a base64-encoded JPEG of the frame to a local Ollama vision model.
    - Default model: gemma3:12b (already pulled, 8.1 GB).
    - Slower (2–10 s per frame) but requires zero additional pip packages
      beyond opencv-python.
    - Prompt engineering: asks the model to choose exactly one word from the
      7-emotion vocabulary. First token of the response is parsed.
    - Override model with: --ollama-model llava:34b

--------------------------------------------------------------------------------
ARCHITECTURE
--------------------------------------------------------------------------------

Threads:
  main thread        — OpenCV webcam capture loop, debounce logic,
                       PureData TCP send, on-screen overlay rendering
  analysis-worker    — Pulls frames from a size-1 queue, runs the chosen
                       backend, writes result to _result dict under a lock
  net-client (per)   — (PDSender) handles each TCP send to PureData

Shared state:
  _frame_q           — queue.Queue(maxsize=1): always holds the most recent
                       frame; older frames are dropped automatically
  _result            — {'mood': str|None, 'confidence': float}, protected
                       by _result_lock
  _stop_evt          — threading.Event() for clean shutdown

--------------------------------------------------------------------------------
KEY FEATURES
--------------------------------------------------------------------------------

Debounce:
  A mood change is only announced after --debounce (default: 2) consecutive
  identical readings. This prevents a single misclassified frame from
  triggering a brief spurious mood change in PureData and the synthesiser.

PureData TCP protocol:
  PDSender maintains a persistent TCP connection.
  Message format:   "happiness ;\n"
  PureData receives this via [netreceive PORT 1] which parses it as a symbol.
  If the connection is lost, PDSender reconnects automatically on the next send.

  PureData patch example:
    [netreceive 3001 1]
    |
    [print]           ← prints e.g.: symbol happiness

On-screen overlay (--no-display disables it):
  - Mood name in a color unique to each emotion
  - Confidence percentage bar (DeepFace backend)
  - Green/red dot indicating PureData connection status

DeepFace pre-warming:
  On startup the analysis worker sends a blank 64×64 black frame through
  DeepFace. This forces TensorFlow and the model weights to load before
  the first real frame arrives, avoiding a multi-second stall in the live loop.

--------------------------------------------------------------------------------
EMOTION LABEL MAPPING
--------------------------------------------------------------------------------

DeepFace labels → program labels:
  'happy'   → happiness
  'sad'     → sadness
  'angry'   → anger
  'fear'    → fear
  'surprise'→ surprise
  'disgust' → disgust
  'neutral' → neutral    ← neutral is now a first-class mood in rhythm_changes.py

The 8-mood Ollama prompt constrains the model to output exactly one of
the canonical mood names, so no mapping is needed for that backend.

--------------------------------------------------------------------------------
COMMAND-LINE INTERFACE
--------------------------------------------------------------------------------

python mood_detector.py [options]

  --pd-host HOST          PureData netreceive host          (default: 127.0.0.1)
  --pd-port PORT          PureData netreceive TCP port       (default: 3001)
  --camera N              Webcam device index                (default: 0)
  --interval F            Seconds between analyses           (default: 0.5)
  --confidence F          Min DeepFace confidence % to accept(default: 25.0)
  --debounce N            Consecutive readings before change  (default: 2)
  --backend BACKEND       deepface | ollama                  (default: deepface)
  --ollama-model MODEL    Ollama vision model                (default: gemma3:12b)
  --ollama-url URL        Ollama API base URL                (default: http://localhost:11434)
  --no-display            Headless mode (no OpenCV window)
  --color-model MODEL     full | bw | duotone                (default: full)
  --detector-backend B    retinaface|opencv|ssd|…            (default: retinaface)
  --crop FRAC             Fraction to discard from each side (default: 0.25)
  --crop-display          Show only the active crop region in the window
  --scale PCT             Resize preview window to PCT%%      (default: 100)

--------------------------------------------------------------------------------
INTEGRATION WITH rhythm_changes.py
--------------------------------------------------------------------------------

These two programs are designed to run simultaneously:

  Terminal 1:
    python rhythm_changes.py --net-port 3000 --mood happiness

  Terminal 2:
    python mood_detector.py --pd-port 3000

mood_detector.py detects emotions from the webcam and sends the mood name to
the TCP port that rhythm_changes.py is listening on. rhythm_changes.py then
adjusts its improvisatory melody in real time to reflect the detected emotion.

Port alignment:
  mood_detector.py  sends   → port 3000  (or whatever --pd-port is set to)
  rhythm_changes.py listens → port 3000  (--net-port)

Note: in this session mood_detector.py defaults to port 3001 (playing safe
with PureData), so when piping directly into rhythm_changes.py both
--pd-port and --net-port should be set to the same value.

--------------------------------------------------------------------------------
INSTALL NOTES
--------------------------------------------------------------------------------

Minimal (ollama backend only):
  pip install opencv-python

Full (deepface backend, recommended):
  pip install opencv-python deepface tf-keras

Models required for ollama backend (already pulled on this machine):
  gemma3:12b   8.1 GB   (default)
  llava:34b    20 GB    (optional, higher accuracy)

--------------------------------------------------------------------------------
UPDATE — Synced to rhythm_changes.py 8-mood vocabulary
--------------------------------------------------------------------------------
After rhythm_changes.py gained a neutral mood (Step 7 in log.txt), mood_detector.py
was updated to match:

  MOODS frozenset      — added 'neutral' (was 7 moods, now 8)
  DEEPFACE_MAP         — 'neutral' → 'neutral'  (was → 'contempt')
  MOOD_COLORS          — added 'neutral': (200, 200, 200)  (light grey)
  Ollama prompt        — lists all 8 moods including neutral
  Module docstring     — updated emotion list

The previous workaround (routing DeepFace's 'neutral' label to 'contempt' as the
closest Ekman substitute) is no longer needed now that neutral is a dedicated mood
with its own MOOD_PARAMS entry in rhythm_changes.py.

--------------------------------------------------------------------------------
UPDATE — Face-gated playback
--------------------------------------------------------------------------------
mood_detector.py now signals face presence/absence to rhythm_changes.py over
the same TCP connection used for mood names.

New TCP tokens sent by mood_detector.py:
  face_off   — sent immediately on startup (rhythm_changes.py starts muted)
  face_on    — sent when a face is confirmed present (after debounce)
  face_off   — sent when the face is confirmed gone  (after debounce)

Changes in mood_detector.py:
  _result dict gains 'face_present' (bool), set by the analysis worker:
    True  when the backend returns a valid mood (implying a face was found)
    False when the backend returns None (no face or below confidence)
  Separate face-presence debounce in the main loop, using the same
    --debounce count as mood debounce, to avoid flickering on/off.
  Console prints '[face detected]' / '[face lost]' on each transition.
  Overlay shows 'NO FACE' in dark grey when no face is confirmed.
  PDSender.send() is reused for face tokens; the same connection carries
    both mood names and face-presence signals.

--------------------------------------------------------------------------------
UPDATE — face_off on shutdown + enforce_detection fix
--------------------------------------------------------------------------------
Two follow-up fixes to the face-gating feature:

1. face_off sent on shutdown:
   sender.send('face_off') is now called in the finally block before
   sender.close(), so rhythm_changes.py is always silenced cleanly when
   mood_detector exits (Ctrl+C, q, or Esc).

2. enforce_detection=True in _analyze_deepface:
   The DeepFace call now uses enforce_detection=True instead of False.
   With enforce_detection=False, DeepFace analyses the whole image even
   when no face is present and almost always returns a dominant emotion,
   meaning face_present was effectively stuck True and face_off was never
   sent during a live session.
   With enforce_detection=True, DeepFace raises ValueError when no face
   is found; that exception is caught and returns (None, 0.0), correctly
   setting face_present=False and triggering the face_off send.
   The pre-warm call on startup still uses enforce_detection=False
   (intentionally: the blank frame has no face by design).

--------------------------------------------------------------------------------
UPDATE — --crop: side-cropping for analysis  (May 3, 2026)
--------------------------------------------------------------------------------
Added a --crop parameter so the analysis backends only see the central portion
of each webcam frame, discarding a configurable fraction from the left and
right edges.

New parameter:
  --crop FRAC      Fraction of frame width to discard from each side.
                   0   = full frame (crop disabled)
                   0.25 = discard 25% left + 25% right, analyse middle 50%
                   Default: 0.25

Motivation:
  Facial emotion analysis is most reliable when the face dominates the input.
  Background clutter and movement at the frame edges can introduce noise that
  the analyser may misread. Cropping the sides narrows the field of view to
  the region where the face is most likely to appear.

  DeepFace already performs internal face localisation, so the accuracy gain
  is modest — but reducing irrelevant peripheral content lowers the chance of
  false face detections and distracting background edges.

  Trade-off: if the subject sits significantly off-centre, the face may be
  partially or fully outside the cropped region. In that case, reduce --crop
  or set it to 0 to restore the full frame.

Implementation:
  In the main capture loop, when args.crop > 0 a column-slice of the frame
  is computed (_x1 = int(w * crop), _x2 = w - _x1) and _put_frame() is
  called with frame[:, _x1:_x2] instead of the full frame.
  The display window continues to show the full frame; two cyan vertical guide
  lines mark the crop boundaries so the active analysis region is visible at
  a glance.

  DEFAULT_CROP_SIDES = 0.25 is added alongside the other defaults at the top
  of the file.  Validation: 0.0 <= crop < 0.5 (crop >= 0.5 would leave no
  usable image).

  The start-up console output prints the crop setting when it is non-zero:
    Crop     : 25% from each side  (using middle 50% of the frame)

--------------------------------------------------------------------------------
UPDATE — --color-model: preview colour modes  (May 4, 2026)
--------------------------------------------------------------------------------
Added a --color-model parameter that transforms the display frame before it
is shown in the popup window. Analysis is always performed on the untransformed
frame, so this is a purely cosmetic option.

New parameter:
  --color-model MODEL   full | bw | duotone   (default: full)

  full      Normal colour, no change (existing behaviour).
  bw        Greyscale: the frame is converted BGR → grey → BGR so all three
            channels carry the same luminance value.
  duotone   Two-tone tint: each pixel is linearly interpolated between a
            fixed dark-indigo shadow colour and the current mood colour as the
            highlight. Falls back to amber while no mood is confirmed yet.
            Creates a coloured mood-reactive tint over the image.

Implementation:
  New helper _apply_color_model(frame, color_model, mood_color) returns a
  transformed copy; the original frame is never modified.
  In the main display block the helper is called before _draw_overlay() and
  the result (display_frame) is what gets passed to cv2.imshow().
  The MOOD_COLORS dict (already present) provides the highlight colour for
  duotone mode.

--------------------------------------------------------------------------------
UPDATE — Remove Qt toolbar from popup window  (May 4, 2026)
--------------------------------------------------------------------------------
The OpenCV Qt backend adds a row of pictogram toolbar buttons to the top of
every namedWindow. This is suppressed by creating the window explicitly with
cv2.WINDOW_GUI_NORMAL | cv2.WINDOW_AUTOSIZE before the capture loop starts.

  cv2.namedWindow('Mood Detector  [q / Esc = quit]',
                  cv2.WINDOW_GUI_NORMAL | cv2.WINDOW_AUTOSIZE)

  WINDOW_GUI_NORMAL   — plain window, no Qt toolbar
  WINDOW_AUTOSIZE     — window resizes to match frame dimensions (same as
                        the previous implicit default)

The namedWindow call is skipped entirely in --no-display mode.

--------------------------------------------------------------------------------
UPDATE — --detector-backend: choose DeepFace face detector  (May 4, 2026)
--------------------------------------------------------------------------------
Added a --detector-backend parameter so the DeepFace face-detection algorithm
can be selected at runtime without editing source code.

New parameter:
  --detector-backend BACKEND   (default: retinaface)

  Available choices:
    retinaface   Most accurate, moderate speed (default)
    opencv       Fastest, basic accuracy – good for weaker hardware
    ssd          Fast, good accuracy
    dlib         Moderate speed, good accuracy
    mtcnn        Moderate speed, good accuracy
    fastmtcnn    Faster variant of mtcnn
    yunet        Fast, good accuracy
    yolov8       Moderate speed, very good accuracy

Implementation:
  DEFAULT_DETECTOR_BACKEND = 'retinaface' added to the defaults block.
  detector_backend=args.detector_backend passed to both:
    1. The live analysis call in _analyze_deepface()
    2. The pre-warm DeepFace.analyze() call in analysis_worker()

  Has no effect when --backend ollama is used.

--------------------------------------------------------------------------------
UPDATE — --crop-display: show only the active crop region  (May 4, 2026)
--------------------------------------------------------------------------------
Added a --crop-display flag. When set together with a non-zero --crop value,
the preview window shows only the central strip that is actually sent to the
analysis backend, and the window automatically resizes to that narrower width.

New parameter:
  --crop-display    (flag, default: off)

  When off (default): full frame is shown with cyan guide lines at the crop
    boundaries (existing behaviour).
  When on: the frame is sliced to [x1:x2] before being passed to
    _apply_color_model() and _draw_overlay(), so the popup is exactly as wide
    as the active analysis region. Guide lines are suppressed since the crop
    boundaries are now the window edges.

Implementation:
  The slice is applied to display_frame (after _apply_color_model) so neither
  the original captured frame nor the analysis input is affected.
  cv2.WINDOW_AUTOSIZE on the namedWindow handles the size change automatically.
  Has no visual effect when --crop 0 is used (no crop active).

--------------------------------------------------------------------------------
UPDATE — Centered overlay for --crop-display mode  (May 4, 2026)
--------------------------------------------------------------------------------
When --crop-display is active the preview window shows a narrow strip without
the usual footer bar context of the full-frame view. The standard _draw_overlay()
footer was therefore replaced with a new helper _draw_centered_overlay() that
renders the mood name prominently in the centre of the (already-cropped) frame.

New helper: _draw_centered_overlay(frame, mood, confidence, pd_ok, face_ok)
  Called instead of _draw_overlay() when:  args.crop > 0 and args.crop_display

Text style:
  Font        cv2.FONT_HERSHEY_DUPLEX, scale 1.4, thickness 2
  Color       always white (255, 255, 255) — readable on any background/tint
  Background  none (transparent — no rectangle drawn behind the text)
  Position    baseline at ty = int(h * 0.75) + th // 2
              (centred on the 75%-from-top horizontal line of the frame)
  Horizontal  centred: tx = (w - tw) // 2

PD indicator:
  The green/red dot and "PD" label are moved to the top-right corner:
    dot at (w - 14, 20), "PD" text at (w - 38, 26)
  This keeps the indicator visible without overlapping the centred mood text.

Rationale:
  The full-frame footer overlay would look out of place on the narrow crop
  strip — the colour bar and confidence text take up too much horizontal space.
  A single large label centred near the bottom-third of the frame looks
  cleaner and is easy to read at a glance. White text works across full,
  bw, and duotone color models. The transparent background avoids obscuring
  the face in the frame.

--------------------------------------------------------------------------------
UPDATE — --scale: resize the preview window  (May 5, 2026)
--------------------------------------------------------------------------------
Added an optional --scale parameter that takes an integer percentage and
resizes the popup window accordingly, without affecting the analysis pipeline.

New parameter:
  --scale PCT   integer, default 100
    Scale the preview window to PCT% of its natural size before display.
    e.g.  --scale 75   → 75% (shrink window)
          --scale 150  → 150% (enlarge window)

Motivation:
  The webcam delivers 640×480 frames. On high-DPI monitors or small screens
  this may be too large or too small for comfortable monitoring. --scale lets
  the window be tuned to any size without altering the captured resolution or
  the frame fed to DeepFace / Ollama.

Implementation:
  Immediately before cv2.imshow(), when args.scale != 100, display_frame is
  resized using cv2.resize() with INTER_LINEAR interpolation:

    _nh = max(1, int(h * scale / 100))
    _nw = max(1, int(w * scale / 100))
    display_frame = cv2.resize(display_frame, (_nw, _nh), interpolation=INTER_LINEAR)

  max(1, …) guards against degenerate zero-pixel dimensions at very small
  percentages. The resize is applied after all overlay drawing, so the
  overlay text and confidence bar scale proportionally with the window.
  cv2.WINDOW_AUTOSIZE on the namedWindow handles the new dimensions automatically.
  Has no effect in --no-display mode (no display_frame is produced).

  Validation: --scale must be a positive integer (> 0); a value of 100 skips
  the resize step entirely so no unnecessary processing occurs at the default.

Examples:
  python mood_detector.py --scale 75
  python mood_detector.py --scale 150
  python mood_detector.py --color-model duotone --crop-display --scale 125

================================================================================
END OF LOG
================================================================================
